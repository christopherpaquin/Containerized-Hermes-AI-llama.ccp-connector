# Worklog

## 2026-08-15 23:44
**Task:** Build a reproducible Docker Compose deployment of Hermes Agent (gateway + dashboard) wired to a native, host-run llama.cpp llama-server, with no cloud inference provider ever configured.

**Decision:** Followed NousResearch/hermes-agent's own root `docker-compose.yml` architecture as closely as possible (two services, `network_mode: host`, `~/.hermes:/opt/data`, `HERMES_UID`/`HERMES_GID` defaulting to 10000) but swapped its `build: .` for the published `nousresearch/hermes-agent` image, pinned to `v2026.8.13` (exact tag, not `latest`/`main`, per repo image-pinning policy). Model provider is configured via Hermes's own `hermes config set model.{provider,base_url,default,api_key}` CLI (not hand-edited YAML), run inside the gateway container via `docker exec hermes`.

**Why:** The repo's own compose file is the most authoritative source for "current official architecture" (more so than a docs-page example that showed a different single-container pattern). Using the official `hermes config` CLI avoids re-implementing YAML-merge/migration logic Hermes already owns, and backs up `config.yaml` automatically only when a change is actually about to happen (verified idempotent across 3+ `./deploy.sh` runs - no duplicate backups, no config churn).

**Progress:** Deployed and verified end-to-end on this host: llama.cpp confirmed running with `--jinja` and `n_ctx=65536` (meets Hermes's hard 64K minimum for tool use), `deploy.sh`/`healthcheck.sh`/`status.sh`/`uninstall.sh` all pass `bash -n` + shellcheck clean, `docker compose config` validates, and `docker exec hermes hermes -z "Reply with exactly: HERMES_LLAMA_OK"` succeeded - proving the full Hermes -> llama.cpp -> model path, not just llama.cpp in isolation. `uninstall.sh --purge`'s typed-confirmation guard was verified to abort safely on a mismatched confirmation. No cloud provider API key is ever written to `~/.hermes/.env`; documented in README why that (plus `auxiliary.*` defaulting to the main/local model) is the actual guardrail against accidental cloud fallback, since Hermes's `fallback_providers` is opt-in and not touched by this deployment.

## 2026-08-16 03:37
**Task:** User asked to expose the Hermes dashboard on 0.0.0.0 (LAN) instead of the 127.0.0.1-only default.

**Decision:** Added optional `HERMES_DASHBOARD_BASIC_AUTH_USERNAME`/`_PASSWORD`/`_SECRET` passthrough in compose.yaml, and a `check_dashboard_auth` step in deploy.sh that (a) refuses to proceed with a non-loopback `HERMES_DASHBOARD_HOST` unless username+password are already set in `.env`, and (b) auto-generates `HERMES_DASHBOARD_BASIC_AUTH_SECRET` via `openssl rand -hex 32` the first time it's needed, idempotently (verified stable across a repeat `./deploy.sh` run). healthcheck.sh and status.sh were updated to probe `127.0.0.1` instead of a literal `0.0.0.0` destination (kernel treats 0.0.0.0 as non-connectable as a destination in some contexts) via a new `dashboard_probe_host`/`is_loopback_host` helper pair in common.sh, and healthcheck.sh now asserts an unauthenticated request against a non-loopback bind does NOT return HTTP 200 -- i.e. that the auth gate is actually enforced, not just configured.

**Why:** Hermes hard-refuses to start a non-loopback dashboard without a registered auth provider (a documented response to a real prior incident: an unauthenticated public dashboard was the entry point for a backdoor campaign). Rather than let deploy.sh fail opaquely inside the container, it now fails fast on the host with a clear message telling the operator what env vars to set. `.env.example` keeps `HERMES_DASHBOARD_HOST=127.0.0.1` as the committed default -- LAN exposure is opt-in per-deployment via the gitignored `.env`, so the secure default is preserved for anyone else who clones this repo.

**Progress:** Redeployed with `HERMES_DASHBOARD_HOST=0.0.0.0` and user-supplied basic-auth credentials (username `admin`; the user chose their own password after being warned it should be strong -- they picked a weak one anyway, noted back to them but not overridden). Verified: dashboard now returns HTTP 302 (redirect to a session/cookie-based `/login` page) instead of 200 for unauthenticated requests, `docker compose config` validates, shellcheck clean, and a second `./deploy.sh` run left the generated secret unchanged (idempotent). Did not script-verify the actual login form POST (field names undocumented) -- only that the gate itself is active.

## 2026-08-16 12:51
**Task:** Enable browser automation and the gateway's own OpenAI-compatible API server (LAN-reachable), per user's stated interest in switching CLI coding clients between raw llama.cpp and Hermes-the-agent, plus document that distinction in the README.

**Decision:** Added `shm_size: "1gb"` to the gateway service (Playwright/Chromium needs real /dev/shm, not Docker's 64MB default). Added `API_SERVER_ENABLED`/`API_SERVER_HOST`/`API_SERVER_KEY` passthrough to compose.yaml, off by default in `.env.example` (`127.0.0.1`), following the exact same pattern as the dashboard basic-auth work: a `check_api_server` step in deploy.sh that auto-generates `API_SERVER_KEY` via `openssl rand -hex 32` the first time the server is enabled, idempotently. healthcheck.sh now hits `/v1/models` on the gateway API server twice -- once with no credentials (expect 401) and once with the generated key as a Bearer token (expect 200) -- to prove the auth boundary is real, not just configured.

**Why:** Same reasoning as the dashboard: Hermes's gateway API server (port 8642) is a separate thing from llama.cpp's raw OpenAI-compatible endpoint -- it routes requests through the full Hermes agent loop (memory/skills/tools), so a CLI coding client pointed at it gets agent-framed responses, not bare completions. Documented this distinction explicitly in the README (with the LAN IP obfuscated as `<host-ip>` per user request) so it's clear which endpoint to use for which purpose, rather than the user discovering the difference by trial and error.

**Progress:** Redeployed; healthcheck confirmed the full chain end-to-end on the first attempt (401 unauthenticated, 200 with the generated bearer key against `/v1/models`), and a repeat `./deploy.sh` left `API_SERVER_KEY` unchanged (idempotent, same as the dashboard secret). Did not yet implement: Slack chat-platform wiring (needs the user to create a Slack app/tokens themselves -- instructions given, not yet actioned) or the local-skill-porting survey (user chose "survey first"; Hermes's `SKILL.md` format is close to but not identical to this user's existing Claude Skills -- needs a `version` field, `metadata.hermes.*` namespace, tool references reframed from Claude Code's tool names to Hermes's own -- survey not yet performed against the user's actual skill files).

## 2026-08-16 13:06
**Task:** Wire up Slack as a chat platform for Hermes (user's choice after declining Telegram), using Socket Mode so no inbound port/public webhook is needed.

**Decision:** Added a `configure_slack` step to deploy.sh, gated on `SLACK_BOT_TOKEN` being set in `.env`. `SLACK_BOT_TOKEN`/`SLACK_APP_TOKEN` route correctly through `hermes config set` (Hermes recognizes them as secret-shaped keys and writes them to `~/.hermes/.env`), but `SLACK_ALLOWED_USERS` does not -- `hermes config set SLACK_ALLOWED_USERS ...` silently misfiled it as a stray unrecognized top-level key in `config.yaml` (with a `not a recognized config key` warning) instead of `~/.hermes/.env`, where the docs say it belongs. Fixed by writing `SLACK_ALLOWED_USERS` directly into `~/.hermes/.env` via the same `upsert_env_var` helper used for this repo's own `.env`, and by comparing idempotency against `~/.hermes/.env` directly for that one var (not `hermes config get`, which doesn't know about it either).

**Why:** Trusting the CLI's own "not a recognized config key" warning at face value would have shipped a broken allowlist -- Hermes's default is deny-all-unknown-senders, so a misfilfiled `SLACK_ALLOWED_USERS` could either silently not work, or (worse, unverified either way) accidentally rely on undocumented config.yaml-to-env bridging behavior. Went with the documented-correct location instead of assuming the bridging behavior was reliable.

**Progress:** Deployed with real user-supplied Slack tokens (xoxb-.../xapp-.../member ID collected interactively over several turns). Confirmed via `docker logs hermes` that the "No env user allowlists configured" warning stopped appearing after the fix landed. Also fixed a startup-race: `hermes gateway restart` (triggered by new Slack config) needs the same `wait_for_port` treatment as the initial gateway start before the API server port is reliably reachable -- healthcheck.sh caught this immediately (failed on `Gateway API server port ... not accepting connections`) both before and after the first fix attempt, which is exactly the kind of real regression this healthcheck is meant to catch, not paper over. Third `./deploy.sh` run confirmed fully idempotent: "Slack already configured with the current tokens; skipping", zero restarts, all health checks pass. Surfaced to the user (not yet actioned): Hermes's own gateway log warns that a network-accessible API server (0.0.0.0) combined with `terminal.backend: local` means anyone reaching the API server or Slack bot can run unsandboxed host-user shell commands -- offered to switch to `terminal.backend: docker` given Slack is now an internet-reachable surface, awaiting the user's decision.

## 2026-08-16 13:07
**Task:** Decide whether to switch `terminal.backend` from `local` (unsandboxed) to `docker` (sandboxed) given the gateway's own warning about unsandboxed host-user command execution via the network-accessible API server / Slack.

**Decision:** User declined -- staying on `terminal.backend: local`.

**Why:** The user is the only member of the Slack org/workspace the bot is installed in, so the Slack attack surface reduces to "the user's own account gets compromised," not "an untrusted third party reaches the bot." Combined with `SLACK_ALLOWED_USERS` already restricting the bot to their member ID and the API server requiring `API_SERVER_KEY`, they judged the actual exposure acceptable for their threat model. Not implemented; no code change.

**How to apply:** If the user later adds teammates to that Slack workspace, or the API server's exposure changes (e.g. port-forwarded past the LAN), revisit this -- the original tradeoff assumed single-user-only access.

## 2026-08-16 13:23
**Task:** Port 5 of the user's Claude Code skills (homelab-skills, homelab-ansible-skills, github-cli, docs-maintenance, coding-standards) into Hermes's own skill format, per the user's picks after a full 10-skill survey.

**Decision:** Wrote each as a fresh `~/.hermes/skills/<category>/<name>/SKILL.md` (categories: `homelab/` new, `github/` and `software-development/` reused from Hermes's existing bundled taxonomy) rather than copying the originals verbatim. Changes made to every one: frontmatter trimmed to Hermes's schema (`name`, `description` ≤60 chars ending in a period, `version`, `platforms`, `metadata.hermes.{tags,category}` — dropped `author`/`license`/`related_skills`/tests/docs-generator, which are specific to Hermes's *official in-repo* contribution path documented in its bundled `hermes-agent-skill-authoring` skill, not the user-local `~/.hermes/skills/` path these live on). Hardcoded `~/.claude/skills/...` paths in bodies were rewritten to `~/.hermes/skills/<category>/...`. Renamed `homelab-skills`→`homelab-infrastructure` and `homelab-ansible-skills`→`homelab-ansible` (the original names read oddly as skill catalog entries — "skills" naming a skill). Explicit Claude-tool-name references (`WebFetch`, `Read`, `Edit`) were reframed as capability descriptions rather than named Hermes tools, since Hermes's actual `web` toolset is currently unconfigured (no EXA/PARALLEL/TAVILY/FIRECRAWL key) and there's no Google Drive MCP server wired up — flagged both gaps explicitly in `homelab-infrastructure`'s Prerequisites/Pitfalls rather than pretending the port has equivalent live capability the original Claude Code skill had.

**Why:** `agent-supervisor` and `claude-remote` (the two skills NOT ported) are fundamentally about orchestrating Claude Code specifically (subagent review across coding-agent CLIs; Claude Code's own Remote Control feature) and have no Hermes equivalent — the user picked the 5 that were actually portable. Two supporting scripts were deliberately NOT copied: `docs-maintenance`'s `check-docs-nudge.sh` is a Claude Code Stop-hook emitting Claude Code's specific `{"systemMessage": ...}` hook JSON — Hermes's `hooks/` system is a different, separate mechanism, so this was left as manual per-turn guidance in the skill body instead of a broken automated hook. `github-cli`'s `hook-bootstrap-gh.sh` (a Claude Code PreToolUse hook installer) was skipped for the same reason; only the plain idempotent `bootstrap-gh-cli.sh` installer script was copied. Also surfaced a real deployment-specific constraint in `github-cli`'s Pitfalls: `gh` isn't baked into the `nousresearch/hermes-agent` image, the agent's terminal tool runs as non-root with sudo disabled in this deployment (confirmed via `hermes doctor`), and apt-installed packages don't survive `docker compose up -d` recreating the container (only `/opt/data` persists) — a one-time root install works for the current container but isn't durable; the real fix would be a derived image, which wasn't done since it wasn't asked for.

**Progress:** All 5 confirmed installed and enabled via `docker exec hermes hermes skills list` (0 hub-installed, 76 builtin, **5 local**). Verified functionally, not just structurally: `hermes -z` was asked a github-cli-specific question (repo naming/layout) and a homelab-infrastructure-specific question (the four sources of truth) with no other context, and both answers correctly cited the exact rules/tables from the newly-ported skills verbatim, proving Hermes actually loads and applies them, not just lists them. Ran the deployment's own `healthcheck.sh` afterward — all checks still pass, nothing broken by the skill installs. These skill files live under `~/.hermes/skills/` (Hermes's own persistent runtime data, same as sessions/memories) — outside this deployment repo's tracked files, so nothing to commit here. Not done: the other 3 surveyed skills (`secret-management`, `worklog`, `vm-provision-raptor-lab`) were flagged as "port with more rework" or "port but flag risk" and the user didn't ask for those in this batch.

## 2026-08-16 13:46
**Task:** User asked whether ~/.hermes should be backed up, and whether to use a backup script vs. a private GitHub repo.

**Decision:** Recommended a local/self-controlled backup script over git, and built `backup.sh`. Excludes `home/.cache` (421MB, per-profile subprocess cache: npm/pip downloads) and `lazy-packages/` (97MB, on-demand Python deps for optional features like faster-whisper) -- both pure regenerable cache, confirmed via `du -sh` before deciding what to exclude. Real backup content is ~18MB compressed. Archive created `600`/directory `700`, timestamped, with rotation (`HERMES_BACKUP_KEEP`, default 14) and an optional `HERMES_BACKUP_REMOTE` rsync target for off-host copies.

**Why:** `~/.hermes/.env` and `config.yaml` hold real secrets (Slack tokens, `API_SERVER_KEY`, dashboard basic-auth secret, potential OAuth credentials) and `~/.hermes/sessions`+`memories` are SQLite state -- neither belongs in git, even a private repo (private repos still carry exposure risk: misconfigured visibility, account compromise, caching that outlives a deleted repo -- same reasoning as this session's earlier "why we didn't containerize a public dashboard without auth" calls). Flagged as a future option if wanted: the 5 ported skills specifically ARE plain text with no secrets and would be reasonable to version-control in their own small private repo, separate from the rest of `~/.hermes` -- not done, since it wasn't asked for.

**Progress:** Ran `./backup.sh` for real -- produced an 18MB archive, confirmed via `tar tzf` that it contains `config.yaml`, `.env`, `SOUL.md`, and the skills directories (including the 5 ported skills), and confirmed the excluded cache directories are actually absent from the archive. `bash -n` + shellcheck clean. Not yet done: no cron schedule was installed (README documents the crontab line but user hasn't asked for it to be wired up automatically); `HERMES_BACKUP_REMOTE` is unset (no remote host chosen yet).

## 2026-08-16 13:52
**Task:** Wire up a backup script installable via Hermes's own cron scheduler (user's stated plan: NFS share coming soon, then schedule backups via `hermes cron`), not just the host-side `backup.sh` from the prior turn.

**Decision:** Added a second script, `hermes-cron-backup.sh`, purpose-built to run *inside* the gateway container (not the host) via `hermes cron create --script ... --no-agent`. `deploy.sh` installs it idempotently to `~/.hermes/scripts/backup-hermes-data.sh` on every run (Hermes's cron requires scripts to live under that exact directory) but does NOT register the actual cron schedule -- printed the exact `hermes cron create` command instead, since the NFS destination isn't ready yet and the schedule/delivery target are the user's call, not mine to guess. Added `HERMES_CRON_BACKUP_DEST` (default `/opt/data/backups`, same-volume for now) and `HERMES_CRON_BACKUP_KEEP` to compose.yaml + `.env.example`, plus a commented example NFS volume mount in compose.yaml's gateway service, so switching to real off-volume backup later is a 3-line change (mount + env var + redeploy), not a rewrite.

**Why:** Confirmed via `docker exec -u hermes hermes bash -c 'echo $HOME'` that the container's HOME is `/opt/data` (not the host's `$HOME`), and that `rsync` isn't installed in the image -- so the host-side `backup.sh` (which sources `common.sh`, uses `$HOME/.hermes`, and optionally rsyncs) cannot run unmodified inside the container. A separate, self-contained script was the correct fix, not a shared one with conditional logic.

**Progress:** Hit and fixed a real bug while wiring this up: `check_api_server`'s generated `API_SERVER_KEY` was being silently ignored by the running gateway, which was actually honoring a *different*, older key already persisted in `~/.hermes/.env` (confirmed by testing both keys directly against `/v1/models` -- old key: 200, newly-generated key: 401, even after the container was recreated with the new value injected via compose `environment:`). Root cause: Hermes evidently loads its own persisted `~/.hermes/.env` with override semantics, so a container-env-var-only approach silently loses to whatever's already on disk -- the same class of bug as the earlier `SLACK_ALLOWED_USERS` misrouting, different mechanism. Fixed `check_api_server` to treat `~/.hermes/.env` as authoritative: adopt its existing value if one differs from ours, and always write the resolved key into it directly rather than relying on env-var passthrough alone. Verified via full `healthcheck.sh` pass (including the authenticated-request check) after the fix, and tested `backup-hermes-data.sh` for real via `docker exec` -- produced an 18MB archive with correct contents (config.yaml, the 5 ported skills, etc.), matching the host script's output.

## 2026-08-16 18:41
**Task:** Both backup scripts (host `backup.sh` and container `hermes-cron-backup.sh`) must verify the eventual NFS destination is actually mounted before writing, and email an alert if not.

**Decision:** Added `verify_nfs_mount_or_alert()` + `send_alert_email()` to `common.sh` (used by `backup.sh`), and a self-contained duplicate of both inside `hermes-cron-backup.sh` (the container has no access to this repo's `common.sh`). Both check `mountpoint -q <path>` against a configurable var (`HERMES_BACKUP_NFS_MOUNT` host-side, `HERMES_CRON_BACKUP_NFS_MOUNT` container-side) and, on failure, **abort without writing anything** and email via Gmail SMTP using `curl`'s built-in SMTP client (`smtps://smtp.gmail.com:465`) -- no MTA/mailutils installed, confirmed none existed on this host beforehand (`mail`/`mailx`/`sendmail`/`msmtp`/`ssmtp`/`postfix` all absent, only `curl`). Credentials (`ALERT_SMTP_FROM`, `ALERT_SMTP_APP_PASSWORD` -- a Google App Password, not the account password -- and `ALERT_EMAIL_TO`) live only in the gitignored `.env`, passed through to the container via compose.yaml.

**Why:** The dangerous failure mode being guarded against: if the NFS mount point exists as a directory but isn't actually mounted, `mkdir -p`+`tar` would succeed anyway, writing the "backup" onto local disk underneath the mount point -- indistinguishable from a real off-volume backup until an actual restore is needed and it turns out never left this host. Explicit `mountpoint -q` + abort-and-alert catches this at write time instead. Left `HERMES_BACKUP_NFS_MOUNT`/`HERMES_CRON_BACKUP_NFS_MOUNT` unset in the live `.env` deliberately, even though the NFS entry now exists in `/etc/fstab` (`raptor.lab:/vol00` -> `/mnt/vol00`, confirmed present but not mounted): the actual backup *destination* vars (`HERMES_BACKUP_DIR`/`HERMES_CRON_BACKUP_DEST`) still point at local disk, so enabling the mount check now would just permanently block working local backups over a mount nothing currently depends on. Caught this exact mistake mid-session (had set `HERMES_BACKUP_NFS_MOUNT=/mnt/vol00` while testing) and reverted it once the check was proven working -- the check and the destination need to move to NFS together, not independently.

**Progress:** Tested both failure paths for real, not just read the code: `./backup.sh` correctly aborted (no new archive written) and sent a real email via Gmail SMTP (curl reported success) when pointed at the not-yet-mounted `/mnt/vol00`; same for the container script via `docker exec -e HERMES_CRON_BACKUP_NFS_MOUNT=/opt/backups ... backup-hermes-data.sh` (aborted, no file under `/opt/data/backups`, alert sent). Confirmed the guard doesn't interfere with normal operation: reverted `HERMES_BACKUP_NFS_MOUNT` to empty afterward and re-ran `./backup.sh` successfully. Full `healthcheck.sh` still green after the compose.yaml env additions and a redeploy. Not yet verified: actual delivery into the `robotunderpants@gmail.com` inbox (curl/SMTP-level success confirmed, but visual inbox confirmation is still with the user).

## 2026-08-16 20:05
**Task:** User confirmed receipt of the test alert email and asked for the Hermes-cron backup job to fire weekly, not the daily example from the README.

**Decision:** Registered the job for real: `docker exec hermes hermes cron create '0 3 * * 0' --script backup-hermes-data.sh --no-agent --name hermes-backup` (Sundays 03:00 UTC, no day/time specified by the user beyond "1x per week" so picked a conventional low-traffic default). Confirmed via `hermes cron list` (job id `5697f7b86b02`, next run 2026-08-23T03:00:00Z). Updated README to document the actual registered schedule instead of the earlier daily placeholder, and noted explicitly that the job's schedule/state lives in Hermes's own `~/.hermes/cron/`, not this repo -- `deploy.sh` reinstalling the script on every run does not re-register or touch the schedule.

**Why:** SMTP alert delivery was end-to-end confirmed by the user actually receiving the test email (not just curl reporting success), so it was safe to consider the failure-alert path production-ready and move on to actually scheduling the job, rather than leaving it print-only in the README.

**Progress:** Job is live, writing weekly to `/opt/data/backups` (local disk -- NFS mount still pending the user's own testing, per the "leave the mount-check unset until destination + check move together" decision from the prior entry). No code changes needed for this step, just cron registration + a README correction.

## 2026-08-17 08:54
**Task:** NFS mount (/mnt/vol00, raptor.lab:/vol00) came up owned root:root 0755 -- host backup.sh couldn't write there, and not even `sudo mkdir` worked client-side.

**Decision:** Diagnosed as NFS `root_squash` (the standard default): the server maps any client's root UID to an unprivileged account, so client-side sudo has no real authority over the export. Wrote a self-contained prompt for a separate Claude Code session on raptor.lab (the NFS server) to investigate and fix server-side -- explicitly instructed it NOT to flip to `no_root_squash` (weakens the export for every client) and instead `chown` a `hermes-backups` subdirectory to the matching non-root UID/GID, confirming with the user before changing anything on that shared host.

**Why:** This client has no authority to fix a root_squash export from its own side, sudo or not -- the fix is inherently server-side. Kept the blast radius to a single new subdirectory rather than touching the export root or its ACLs.

**Progress:** User ran the prompt on raptor.lab; verified from this side afterward: `/mnt/vol00/hermes-backups` is now `cpaquin:cpaquin` `700`, write-tested successfully. Switched `HERMES_BACKUP_DIR=/mnt/vol00/hermes-backups` and `HERMES_BACKUP_NFS_MOUNT=/mnt/vol00` in the live `.env` and ran `./backup.sh` for real -- produced a 32MB archive genuinely on the NFS filesystem (confirmed via `df` showing `raptor.lab:/vol00` as the backing filesystem, and `tar tzf` showing correct contents). This is the first real off-volume Hermes backup. README updated to reflect this is now live, not a future step; also did a separate pass standardizing LAN-IP examples in the README to `10.1.10.x` (was a mix of `127.0.0.1` and an abstract `<host-ip>` placeholder) -- confirmed via `ss -tlnp` that llama-server actually binds `0.0.0.0:8080` (LAN-reachable) even though this deployment's internal Hermes<->llama.cpp wiring intentionally stays on `127.0.0.1`, so the README now documents both accurately rather than only the loopback path. The container-side cron backup (`HERMES_CRON_BACKUP_DEST`) is NOT yet switched to NFS -- that needs a compose.yaml volume mount added first (commented example already there) and hasn't been requested yet.
